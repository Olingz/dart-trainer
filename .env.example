# Kopier til .env.local (lokal udvikling) eller sæt i Vercel → Settings → Environment Variables (beta).

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Vercel sætter VERCEL_URL automatisk — behøves ikke manuelt.
